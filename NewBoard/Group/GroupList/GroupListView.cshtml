@model GroupVMList


<div class="panel panel-default">
    <div class="panel-heading">
        Les Groupes
    </div>

    <a class="btn btn-info btn-sm" href="@Url.NewsBoardUrlHelper().Action("Group", "GroupList", "GetCreate")"
       type="button" ns-action-type="simpleGetAction">
        Creer un nouveau Groupe
    </a>


    <div class="panel-body" style="padding:0px">
        <table class="table table-striped table-condensed">
            <thead>
                <tr>
                    <th>Label</th>
                    <th>Permissions</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.Groups.Count; i++)
                {
                    <tr>
                        <td>@Model.Groups[i].Label</td>
                        <td>@Model.Groups[i].Permissions</td>
                        <td>
                            <div class="btn-group" role="group">
                                <button class="btn btn-info btn-sm"
                                        ns-action-url="@Url.NewsBoardUrlHelper().Action("Group", "GroupList", "GetEdit", new { groupId=Model.Groups[i].Id})"
                                        type="button" ns-action-type="simpleGetAction">
                                    Editer
                                </button>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <a class="btn btn-info btn-sm" href="#" ns-action-url="@Url.NewsBoardUrlHelper().Action("Group", "GroupList", "GetCreate")"
       type="button" ns-action-type="simpleGetAction">
        Creer un nouveau Groupe
    </a>
</div>


